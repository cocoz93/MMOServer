<!-- 스냅샷: 2026-08-04T05:58:32Z (A0에서 2곳 수정 후 재취득) · 페이지 ID 3b216a0b9f598154a7d8fcc62d1b4219 · 제목 "🧱 4. 네트워크 밖의 Windows 의존" -->

<callout icon="🧱" color="blue_bg">
	[3. 두 모델 차이가 터진 곳](https://app.notion.com/p/3b216a0b9f598182af58f74c4416bc45)이 네트워크 이야기였다면, 여기는 **그 밖의 전부**다.
	서버는 소켓만 Windows에 묶여 있는 게 아니었다. 타이머·경로·코어 배치·종료 시그널·크래시 덤프·설정 파일·로그 시각까지, **눈에 안 띄는 곳이 더 많았다.** 이 문서는 그것들을 어떻게 한곳으로 몰았는지, 그리고 **몰면 안 되는 것이 하나 있었다**는 이야기다.
</callout>
<callout icon="📌" color="gray_bg">
	**범위** — `IOCP_Server/IOCP_Server/Platform/Platform.h`(신설)와 전송 계층 경계. 락프리 자료구조 쪽 어댑터는 [2. 락프리를 리눅스로](https://app.notion.com/p/3b216a0b9f5981e9b81fe8a20bd89ad3)에 따로 있다.
</callout>
<table_of_contents/>
---
# 1. 무엇이 Windows에 묶여 있었나
소켓을 빼고도 이만큼이었다.
<table fit-page-width="true" header-row="true">
<tr>
<td>**갈래**</td>
<td>**Windows**</td>
<td>**리눅스**</td>
</tr>
<tr>
<td>고해상도 타이머</td>
<td>`timeBeginPeriod`</td>
<td>대응물 없음 — `std::sleep_for`가 이미 충분한 해상도</td>
</tr>
<tr>
<td>실행 파일 경로</td>
<td>`GetModuleFileNameA`</td>
<td>`/proc/self/exe`</td>
</tr>
<tr>
<td>코어 배치</td>
<td>`SetThreadAffinityMask` · `GetProcessAffinityMask`</td>
<td>`pthread_setaffinity_np` · `sched_getaffinity`</td>
</tr>
<tr>
<td>종료 시그널</td>
<td>`SetConsoleCtrlHandler`</td>
<td>`sigaction`(SIGINT·SIGTERM)</td>
</tr>
<tr>
<td>스레드 CPU 시간</td>
<td>`GetThreadTimes` (커널·유저 분리)</td>
<td>`clock_gettime`(per-thread) — **커널분을 따로 못 준다**</td>
</tr>
<tr>
<td>32비트 원자연산</td>
<td>`InterlockedIncrement` 계열</td>
<td>`__atomic_*`</td>
</tr>
<tr>
<td>설정 파일</td>
<td>`GetPrivateProfileString` 계열</td>
<td>대응물 없음 — **직접 파서를 만들었다**</td>
</tr>
<tr>
<td>로그 시각</td>
<td>`localtime_s`</td>
<td>`localtime_r`</td>
</tr>
<tr>
<td>크래시 덤프</td>
<td>미니덤프(`MiniDumpWriteDump`)</td>
<td>`sigaction` · `backtrace` — **4장에서 따로**</td>
</tr>
</table>
# 2. Platform — 한곳에 가두기
이 아홉 갈래를 헤더 하나(`Platform.h`)에 몰았다. 호출부는 `Platform::SetCurrentThreadAffinity(...)`처럼 부르고, **어느 OS인지 모른 채로 돈다.**
```c++
// 호출부는 이렇게만 보인다
Platform::SetHighResolutionTimer(true);
Platform::InstallShutdownHandler(cb, &completeFlag, 4500);
const int cores = Platform::GetAvailableCoreCount();
```
<callout icon="🔑" color="yellow_bg">
	가두는 목적은 "`#ifdef`를 없애는 것"이 아니다. **`#ifdef`****를 한 파일에만 두는 것**이다.
	`#ifdef`가 코드 곳곳에 흩어져 있으면, 새 OS를 붙일 때 무엇을 빠뜨렸는지 알 방법이 없다. 한 파일에 모여 있으면 그 파일만 훑으면 된다 — 실제로 리눅스 팔을 채울 때 이 헤더가 **할 일 목록** 역할을 했다.
</callout>
## 두 곳은 그냥 못 옮겼다
<callout icon="⚠️" color="orange_bg">
	**① 커널 CPU 시간이 사라진다.**
	Windows `GetThreadTimes`는 커널 시간과 유저 시간을 나눠 준다. 우리는 그걸로 `mmo_thread_kernel_ratio`(스레드가 커널에 머문 비율)를 재고 있었다.
	리눅스의 per-thread 시계는 **합계만** 준다. 그래서 리눅스에서는 이 지표가 **0으로 고정**된다. 값을 지어내는 대신 "못 재는 것"으로 남겼다 — 나중에 이 지표를 볼 때 리눅스 숫자를 IOCP와 나란히 놓으면 안 된다.
</callout>
<callout icon="⚠️" color="orange_bg">
	**② 창을 닫을 때의 정리 시간.**
	Windows에서 콘솔 창을 닫거나 로그오프하면, `SetConsoleCtrlHandler`의 핸들러가 **반환하는 순간 OS가 프로세스를 죽인다.** DB 최종 저장이 잘릴 수 있다.
	그래서 `InstallShutdownHandler`에 완료 플래그와 최대 대기 시간을 받는 인자를 더했다 — 핸들러가 "정리 끝났다"는 신호를 기다렸다가 반환한다. 리눅스 `sigaction` 경로에는 이런 제약이 없어 인자를 받아도 그냥 통과시킨다.
</callout>
## 함정 — "한곳에 가둔다"가 항상 옳지는 않았다
소켓 헤더까지 `Platform.h`에 넣으려다 **빌드가 통째로 깨졌다.**
Windows 소켓 헤더에는 순서 규칙이 있다. `<WinSock2.h>`가 `<Windows.h>`보다 **먼저** 와야 한다. 안 그러면 `<Windows.h>`가 끌고 오는 옛 `winsock.h`와 충돌해 `sockaddr` 재정의 오류가 난다.
`Platform.h`에 `<WinSock2.h>`를 넣자, 이 헤더를 포함하지 않는 다른 번역 단위들이 여전히 `<Windows.h>`를 먼저 열면서 그 충돌이 났다. `WIN32_LEAN_AND_MEAN`으로도 못 막았다 — **그 매크로가 없는 파일들이 문제였기 때문이다.**
<callout icon="🕳️" color="red_bg">
	결국 **소켓 헤더 책임은 ****`IOCPServer.h`****로 되돌렸다.** Platform은 소켓 *핸들 타입*(`Platform::NetSocket`)과 *동작*(`CloseSocket`·`WouldBlock`)만 가리고, 헤더 포함 순서는 원래 있던 자리가 지킨다.
	교훈은 이렇다 — **격리 계층은 "무엇을 가릴지"만이 아니라 "무엇을 가리면 안 되는지"도 정해야 한다.** 헤더 포함 순서처럼 번역 단위 전체에 걸리는 제약은 한곳에 몰 수 있는 종류가 아니다.
</callout>
# 3. 전송 경계 15개
Platform이 저수준 동작을 가렸다면, **전송 계층 경계**는 "제출·수거 방식"을 가린다. 공통 골격이 이 15개만 호출하고, 세 팔이 각각 구현한다.
<table fit-page-width="true" header-row="true">
<tr>
<td>**갈래**</td>
<td>**함수**</td>
</tr>
<tr>
<td>준비·정리</td>
<td>`TransportInitSessionBuffer` · `TransportPreListen` · `TransportPreListenCleanup` · `TransportPostListen` · `TransportListenFlags`</td>
</tr>
<tr>
<td>워커 수명</td>
<td>`TransportStartWorkers` · `TransportLogStarted` · `TransportStopBeforeDrain` · `TransportStopAfterDrain`</td>
</tr>
<tr>
<td>세션 연결</td>
<td>`TransportAttachSession` · `TransportStartFirstRecv` · `TransportRequestDisconnect`</td>
</tr>
<tr>
<td>송신</td>
<td>`TransportSendImmediate` · `TransportFlushDirty` · `TransportSubmitSegment`</td>
</tr>
</table>
세 팔이 전부 **15개씩** 구현한다 — `Transport_Iocp.cpp` · `Transport_Rio.cpp` · `Transport_Epoll.cpp`.
## 이 경계를 만드느라 Windows 코드도 고쳤다
<callout icon="🔑" color="yellow_bg">
	**"리눅스만 새로 얹었다"가 아니다.** 경계를 OS 중립으로 만들려면 `SOCKET`·`DWORD` 같은 Windows 타입이 시그니처에서 빠져야 하고, 그러려면 **기존 두 팔의 구현도 같이 맞춰야** 한다.
	실제로 `Transport_Iocp.cpp` 63줄, `Transport_Rio.cpp` 30줄이 바뀌었다. 그래서 **Windows 양쪽 팔 회귀 빌드가 필수**가 됐다 — 자세한 것은 [5. 검증](https://app.notion.com/p/3b216a0b9f5981dcb3c2cbdf638173f1)에 있다.
</callout>
<callout icon="🕳️" color="gray_bg">
	**경계 하나가 집계에서 빠져 있었다.**
	처음 경계를 세었을 때 **14개**로 셌다. `TransportSubmitSegment` 하나가 나머지 열넷과 **떨어진 자리**(`IOCPServer.h:398`, 나머지는 `:409-422`)에 선언되어 있어 블록만 보고 지나쳤기 때문이다.
	그래서 이 함수만 `SOCKET` 타입이 남았는데 — **빌드는 통과했다.** `Platform.h`가 `using SOCKET = int` 별칭을 두고 있어서다. **동작은 멀쩡한데 의도만 어긋난** 상태였고, 이런 건 컴파일러가 잡아주지 않는다. 나중에 전수 재검증에서 찾아 고쳤다.
</callout>
# 4. 크래시 덤프 — 죽는 방식 자체가 다르다 ★
여기가 이 문서에서 가장 손이 많이 간 곳이다. **`#ifdef`**** 하나로 갈라지지 않는다** — 두 OS가 프로세스를 죽이는 방식과, 죽은 뒤 무엇이 남는지가 통째로 다르기 때문이다.
<table fit-page-width="true" header-row="true">
<tr>
<td></td>
<td>**Windows**</td>
<td>**리눅스**</td>
</tr>
<tr>
<td>잡는 방법</td>
<td>구조적 예외 처리(SEH) — `SetUnhandledExceptionFilter`</td>
<td>시그널 — `sigaction`(SIGSEGV·SIGABRT 등)</td>
</tr>
<tr>
<td>남기는 것</td>
<td>미니덤프 `.dmp` — 메모리·스레드·모듈 전부</td>
<td>스택 주소 목록 — `backtrace`</td>
</tr>
<tr>
<td>이름을 어디서 얻나</td>
<td>PDB(별도 파일)</td>
<td>실행 파일의 **동적 심볼 테이블**</td>
</tr>
<tr>
<td>사후 분석</td>
<td>덤프를 디버거로 연다</td>
<td>주소를 `addr2line`으로 푼다</td>
</tr>
</table>
## `-rdynamic` — 이름이 안 나오던 이유
리눅스 쪽을 처음 붙였을 때, 크래시는 잡히는데 **스택에 주소만 찍혔다.**
원인은 링크 옵션이었다. `backtrace_symbols_fd`가 이름을 찍으려면 그 심볼이 **동적 심볼 테이블에 올라가 있어야** 하는데, 기본 링크는 실행 파일의 심볼을 내보내지 않는다. 라이브러리가 아니니 내보낼 이유가 없다고 보는 것이다.
`-rdynamic`을 주면 내보낸다. 효과를 대조로 확인했다.
<table fit-page-width="true" header-row="true">
<tr>
<td>**링크**</td>
<td>**동적 심볼 수**</td>
</tr>
<tr>
<td>`-rdynamic` 없음</td>
<td>1개</td>
</tr>
<tr>
<td>`-rdynamic` 있음</td>
<td>**100개**</td>
</tr>
</table>
<callout icon="⚠️" color="orange_bg">
	**그래도 안 나오는 것이 있다.** `static` 함수와 익명 네임스페이스 안의 함수는 `-rdynamic`으로도 이름이 안 뜬다 — internal linkage라 **애초에 동적 심볼 테이블에 올릴 수 있는 대상이 아니다.**
	이건 옵션으로 해결되지 않는 종류의 한계다. 그 주소는 사후에 풀어야 한다.
	```bash
addr2line -f -C -e <실행파일> 0x12e8   # 함수명 + 소스:줄번호
	```
</callout>
## 핸들러 안에서는 `malloc`을 부르면 안 된다
시그널 핸들러는 아무 때나 끼어든다. **`malloc`**** 안에서 죽었는데 핸들러가 다시 ****`malloc`****을 부르면** 그대로 멈춘다(교착). 그런데 `backtrace`는 첫 호출에서 libgcc를 지연 로딩하며 할당을 한다.
그래서 **정상일 때 미리 한 번 불러 둔다.**
```c++
backtrace(warm, 4);   // libgcc 예열 → 핸들러 내 최초 malloc 회피
```
같은 이유로 핸들러 안에서는 `printf`가 아니라 `write`를 직접 쓴다 — 시그널 안에서 불러도 안전한 함수만 쓰기 위해서다.
## 마지막에 기본 동작으로 되돌린다
```c++
::signal(sig, SIG_DFL);   // 기본 동작으로 재발생 → 코어덤프/종료
```
스택을 남긴 뒤 **일부러 다시 죽는다.** 그래야 OS가 코어덤프를 만들고, 종료 코드도 "시그널로 죽었다"로 제대로 남는다. 핸들러에서 그냥 `exit`하면 그 정보가 사라진다.
<callout icon="🕳️" color="red_bg">
	**검증 중 걸린 함정 — ****`-O2`****에서는 크래시가 안 났다.**
	SIGSEGV 경로를 확인하려고 `volatile int* p = nullptr; *p = 1;`을 넣었는데, **`-O2`****가 이 줄을 통째로 지워버렸다.** 널 역참조는 정의되지 않은 동작이라 컴파일러가 "일어나지 않는다"고 가정하고 없앨 수 있다.
	그런데 크래시 로그는 찍혔다 — **그 다음 줄의 ****`SIGABRT`**** 경로가 잡힌 것이었다.** 그대로 넘어갔으면 "SIGSEGV 경로가 동작한다"고 **잘못 기록할 뻔했다.**
	정의되지 않은 동작을 일부러 일으켜 확인하는 시험은 **`-O0`****으로 해야 한다.**
</callout>
# 5. 나머지 — 표 한 줄씩
각각은 "옮겼다" 이상의 이야기가 없어 줄로만 남긴다.
<table fit-page-width="true" header-row="true">
<tr>
<td>**무엇**</td>
<td>**어떻게**</td>
</tr>
<tr>
<td>설정 파일</td>
<td>`GetPrivateProfileString` 계열은 Windows 전용이라 **119줄짜리 파서를 직접 만들었다**(`IniFile.h`). INI는 형식이 단순해 이게 이식보다 싸다</td>
</tr>
<tr>
<td>로그 시각</td>
<td>`localtime_s`(Win) ↔ `localtime_r`(Linux) — 인자 순서가 반대라 `#ifdef`로 갈랐다 (`Logger.cpp:45-47`)</td>
</tr>
<tr>
<td>모니터 CPU 측정</td>
<td>`GetThreadTimes` ↔ `clock_gettime`. **커널/유저 분리는 리눅스에서 못 한다**(2장 참고)</td>
</tr>
<tr>
<td>통계 카운터</td>
<td>`Interlocked*` → `std::atomic`(relaxed). 세션 상태 플래그는 제외 — 그쪽은 순서가 의미를 갖는다</td>
</tr>
<tr>
<td>스칼라 타입</td>
<td>`DWORD`·`LONG` 등 75곳을 `cstdint` 고정폭으로. `DWORD = unsigned long`이 리눅스에서 **8바이트**가 되는 것이 이유다</td>
</tr>
<tr>
<td>채팅 문자 폭</td>
<td>`wchar_t`가 Windows 2바이트 / 리눅스 4바이트라 **와이어 포맷이 어긋난다**. `char16_t`로 고정해 양 OS에서 같은 크기가 되게 했다 (`Protocol.h:210`)</td>
</tr>
</table>
<callout icon="🔑" color="yellow_bg">
	마지막 줄이 **크로스 OS 통신의 전제**다. 문자 하나가 2바이트냐 4바이트냐가 구조체 크기를 바꾸고, 그러면 Windows 클라이언트가 리눅스 서버에 붙는 순간 패킷이 어긋난다.
	`char16_t`는 표준이 **정확히 16비트**를 보장한다. 바꾼 뒤 양 OS에서 구조체 크기가 같은지 확인했고(`MSG_S2C_CHAT` 1034바이트), Windows에서 빌드한 기존 부하 클라이언트가 리눅스 서버와 그대로 통신했다 — [5. 검증](https://app.notion.com/p/3b216a0b9f5981dcb3c2cbdf638173f1) 참고.
</callout>
# 6. 소스 지도
<table fit-page-width="true" header-row="true">
<tr>
<td>**무엇**</td>
<td>**어디**</td>
</tr>
<tr>
<td>플랫폼 격리 계층</td>
<td>`Platform/Platform.h` (신설)</td>
</tr>
<tr>
<td>전송 경계 선언 15개</td>
<td>`IOCPServer.h:398`, `:409-422`</td>
</tr>
<tr>
<td>크래시 덤프 리눅스 분기</td>
<td>`Crash/CrashDump.h:302` 이하 (`backtrace` 예열 `:321`, 핸들러 등록 `:330`)</td>
</tr>
<tr>
<td>`-rdynamic`</td>
<td>`CMakeLists.txt` UNIX 분기 (전역 `add_link_options`)</td>
</tr>
<tr>
<td>INI 파서</td>
<td>`IniFile.h` (신설, 119줄)</td>
</tr>
<tr>
<td>채팅 문자 폭</td>
<td>`Shared/Protocol/Protocol.h:210`</td>
</tr>
</table>
---
## 직접 확인해 보기
**① ****`-rdynamic`****이 실제로 심볼을 늘리는지**
```bash
objdump -T Run/bin/mmo_server | wc -l   # 동적 심볼 테이블
# 지금 빌드에서 2,200줄. -rdynamic 없이 링크하면 자릿수가 달라진다
# (readelf·nm은 이 WSL에 안 깔려 있다 — objdump만 쓴다)
```
**② ****`static`**** 함수는 왜 이름이 안 나오는지**
```bash
objdump -t Run/bin/mmo_server | grep -c ' l  *F \.text'   # l = local(internal linkage)
# 193개. 이 함수들은 크래시 스택에 주소로만 찍힌다 — addr2line이 필요한 이유
```
**③ 채팅 구조체 크기가 양 OS에서 같은지** — 리눅스에서:
```bash
cat > /tmp/sz.cpp <<'EOF'
#include "Shared/Protocol/Protocol.h"
#include <cstdio>
int main(){ printf("%zu %zu\n", sizeof(MSG_C2S_CHAT), sizeof(MSG_S2C_CHAT)); }
EOF
g++-13 -I. /tmp/sz.cpp -o /tmp/sz && /tmp/sz
# 1028 1034 — Windows에서 재도 같은 값이 나와야 한다
```
**④ 크래시 경로를 직접 밟아 보려면** — 반드시 `-O0`으로. `-O2`는 널 역참조를 지운다.
