# 문서 감사 자가검증

`LINUX_PORT_DOC_AUDIT.md`로 돌린 **전수감사(A0~A7) 자체를 다시 검사한다.**

감사는 24건을 처리했지만, **감사가 스스로 만든 오류도 5건**이었다. 그중 셋은 페이즈 안의
자가검증 ②에서만 잡혔다 — 그 절차가 없었으면 멀쩡한 문서에 오타를 심고 끝났을 것이다.
같은 확률로 **아직 안 잡힌 것이 남아 있다고 보는 편이 맞다.**

여기서 검사하는 것은 문서가 아니라 **내가 문서에 한 일**이다.

## 검사 대상

| 갈래 | 수 | 무엇 |
|---|---|---|
| **노션을 고친 자리** | 26곳 | 아래 표 |
| **새로 써 넣은 주장** | 2건 | 4-H 세션 타임아웃 콜아웃(p3 1장) · 5-A 포트포워딩 문단(p5 3장) |
| **취소한 지적** | 3건 | 들여쓰기(A0) · CAS128 5곳(A1) · objdump 2,200줄(A2) |
| **남긴 판정** | 4건 | 설명 주석 · addr2line 예시 · char16_t 16비트 · "측정 보류" |
| **감사 범위 밖 수정** | 1건 | `LINUX_PORT_PLAN.md:259` (TSan rc) |
| **"문제 없음" 판정** | 표본 | A1 인용 74·A2 수치 40여·A6 과장 11 중 표본 재확인 |

### 노션을 고친 자리 26곳

| # | 페이즈 | 페이지·자리 | 무엇을 |
|---|---|---|---|
| 1-2 | A0 | p4 1장 표 크래시덤프 행 | `•`→`·` · "5장"→"4장" |
| 3 | A0 | p4 직접확인② | "찍힐다"→"찍힌다" |
| 4 | A0 | epoll 범위 콜아웃 | "부졌는지"→"부쉈는지" *(2차 시도로 성공)* |
| 5 | A0 | p3 5장 콜아웃 | "적어 둑다"→"적어 뒀다" *(2차 시도로 성공)* |
| 6 | A1 | p4 2장 코드 예시 | `3000`→`4500` |
| 7 | A2→A3 | p5 직접확인② | `rc=66`→`rc=0` **→ 되돌림** |
| 8 | A3 | epoll 직접확인② | spdlog 문구 |
| 9-10 | A3 | p2 직접확인③④ | windows.h 문구 · "11줄" |
| 11-12 | A3 | p5 2장 콜아웃 | "종료 코드가 0"→"66·구분 안 됨" · "통과로"→"돌렸다로" |
| 13 | A3 | p1 4장 교훈 표 | "종료 코드가 0"→"같은 종료 코드" |
| 14 | A4 | epoll 6장 콜아웃 | `_sendSubmitBusy` 서술 |
| 15 | A4 | epoll 3장 대응표 | `BindIoCompletionCallback`→`CreateIoCompletionPort` |
| 16-18 | A4 | p3 5장 | 제목 · 도입 · 본문 마지막 |
| 19 | A4 | p3 6장 소스 지도 | "새 잠금"→"제출 잠금(공통 골격)" |
| 20 | A5 | epoll 6장 | "두 겹"→"세 겹" |
| 21-22 | A5 | p5 4장 콜아웃 | "리눅스" 명시 · 부연 추가 |
| 23 | A6 | p3 1장 목록 | "· 세션 타임아웃" *(1차 조용히 실패)* |
| 24 | A6 | p3 1장 | ✅ 콜아웃 **신설** |
| 25 | A6 | p5 3장 | 포트포워딩 문단 **신설** |
| 26 | A6 | p5 3장 | "적어 둔데"→"적어 뒀는데" |

*표시가 붙은 셋은 **한 번 실패했다가 다시 넣은 자리**다. 가장 의심스럽다.*

> **26과 17은 세는 단위가 다르다** — 여기 26은 **편집 조각**(`content_updates` 한 개)이고,
> A7 결산의 "고침 17건"은 **발견 건수**다. 한 발견이 여러 자리를 고치기도 했다
> (예: A3의 TSan rc 1건이 네 자리 + 원본 문서 1곳). 둘 다 맞는 숫자다.

## 이 문서를 쓰는 법 (루프 작업 규칙)

**이 문서가 진행 상태의 정본이다.** 시작할 때 여기부터 읽고, 체크가 안 된 첫 페이즈 **하나만** 한다.
절차는 원 감사와 같다 — 작업 → 판정 → **그 자리에서 고치기** → 자가검증 → 기록 → 커밋(push 안 함).

### 재귀를 여기서 끊는다 ★

자가검증의 자가검증은 끝이 없다. **C 감사는 없다.** 대신 이 규칙으로 닫는다.

- **고친 것은 그 자리에서 노션 렌더로 확인한다.** `update`의 성공 응답은 반영의 증거가 아니다
  (A6에서 실제로 성공을 반환하고 반영되지 않았다)
- **한글은 절대 `\uXXXX`로 조립하지 않는다.** 이번 감사에서 세 번 틀렸다.
  넣을 문구는 **먼저 로컬에서 코드포인트를 찍어 확인**하고 넣는다
- **고칠 일이 생기면 가장 작은 단위로 바꾼다.** 새로 쓰면 새 오류가 들어올 자리가 생긴다.
  판정이 뒤집혀 문장을 손대야 할 때도 **문단을 다시 쓰지 말고 어긋난 부분만** 바꾼다
- B가 끝나면 **정지한다.** 남은 의심은 「관측대기」로 적고 다음 통독 때 본다

### 판정을 뒤집는 것도 정상 결과다

- 원 감사가 **고친 것이 틀렸다면** 되돌린다 (A2→A3에서 이미 한 번 했다)
- 원 감사가 **취소한 것이 사실은 결함이었다면** 다시 잡는다
- 원 감사가 **남긴 것이 고쳐야 할 것이었다면** 고친다
- **아무것도 안 뒤집히는 것도 정상이다.** 억지로 찾지 않는다

---

## B0. 반영 대조 — 노션과 스냅샷이 같은가

- [x] **B0** 노션 6페이지를 다시 받아 `docs/_audit_snapshot/`과 **기계로 대조**한다
  - 스냅샷은 감사 내내 **내가 손으로 갱신**해 왔다. 둘이 어긋나 있다면 그 자체가 결함 신호다
  - 차이가 나오면 넷 중 하나로 분류한다
    · **노션 미반영** — 스냅샷에만 있다 (23번이 이 종류였다)
    · **스냅샷 누락** — 노션에만 있다 (기록이 실제와 다르다는 뜻)
    · **엉뚱한 자리** — 둘 다 있는데 내가 의도하지 않은 곳이 바뀌었다
    · **내가 안 한 변경** — 감사 중에 **사용자가 직접 고쳤을** 수 있다.
      이건 결함이 아니므로 **되돌리지 말고 스냅샷만 맞춘다**
  - 스냅샷 첫 줄의 "(A0에서 …곳 수정 반영)" 주석도 실제 이력과 맞는지 본다 — 뒤 페이즈에서
    더 고쳤는데 주석은 그대로인 파일이 있다
  - 받은 본문은 **비교용 임시 파일**에 두고, 판정이 끝난 뒤에만 스냅샷을 맞춘다
  - **판정**: 설명되지 않는 차이가 0인가

## B1. 새로 쓴 문장 통독 — 글자가 성한가

- [x] **B1** 이번 감사에서 **노션에 들어간 문장 26곳**을 눈으로 읽는다
  - 이스케이프 조립으로 **세 번 틀렸다**(부셸는지 · 눴다 · 둔데). 전부 도구는 성공을 반환했고
    **매칭도 정상이라 에러로는 안 드러났다**
  - 특히 **다시 넣은 셋**(4·5·23번)과 **새로 쓴 둘**(24·25번)을 먼저 본다 — 문장이 길수록 위험하다
  - 조사·어미까지 본다. "적어 **둔데**"는 매칭도 렌더도 정상이었고 **읽어야만** 드러났다
  - **판정**: 글자 손상 0, 어색한 어미 0

## B2. 새 주장 근거 재확인 — 내가 넣은 사실이 맞는가

- [x] **B2** 감사가 **새로 써 넣은 주장**을 코드로 다시 확인한다
  - 원 감사에서 나는 문서의 주장을 검증했는데, **내가 넣은 주장은 나 말고 검증한 사람이 없다**
  - 대상
    · **4-H 콜아웃**(p3 1장) — `TimingWheel.h`가 정말 `std::thread`·`steady_clock`·`sleep_for`만 쓰는가 ·
      `IOCPServer.cpp:477`(등록)·`:494`(갱신)이 맞는가 · `SESSION_TIMEOUT_SEC = 60`이 `IOCPServer.h:535`인가 ·
      **"70초 뒤 무응답만 끊겼다"는 원본 기록과 같은가**
    · **5-A 문단**(p5 3장) — `localhostForwarding` 서술이 원본(`PLAN` 5-A)과 어긋나지 않는가
    · **`_sendSubmitBusy` 서술**(epoll 6장·p3 5장) — "IOCP 팔에서 먼저 필요했다"의 근거가 여전히 서는가
    · **A3에서 바꾼 기대값 셋** — spdlog 0건 · windows.h · "11줄"이 지금도 그대로 나오는가
    · **TSan `rc=66`** — 이번 감사에서 **두 번 뒤집힌 값**이다(문서 66 → A2가 0으로 → A3가 66으로).
      노션 네 곳과 `PLAN:259`가 지금 **한 방향으로 정리돼 있는지**까지 본다
    · **`4500`**(p4 2장) — A1에서 3000을 고친 값. `main.cpp:31`이 지금도 4500인가
  - **판정**: 근거를 못 대는 주장이 0인가

## B3. 판정 재검토 · 결산

- [x] **B3** 취소·남김·통과 판정을 다시 보고 결산한다
  - **취소 3건** — 취소가 맞았나. 특히 objdump 2,200줄은 *"1,936은 그때 빌드의 값"*이라고 넘겼는데,
    그 설명이 실제로 성립하는가
  - **남긴 4건** — 남긴 이유가 지금도 유효한가
  - **범위 밖 수정 1건** — `LINUX_PORT_PLAN.md:259`를 고친 문장이 실측과 정확히 맞는가
  - **"문제 없음" 표본** — A1·A2·A6이 통과시킨 것 중 **무작위가 아니라 위험한 쪽**으로 골라 다시 본다
    (여러 곳에 되풀이되는 수치, 근거가 저장소 밖인 값)
  - **결산 숫자** — A7의 24건(고침 17·취소 3·남김 4)이 실제 기록과 맞는가
  - **감사 기록의 수치** — 페이즈 본문에 적은 집계가 실제와 맞는가.
    "줄번호 74 · 심볼 52 · 블록 9"(A1) · "`bash` 블록 18개"(A3) · "5회 재현"(A3) ·
    "완료 단계 24개"(A6) — **감사가 자기 작업량을 부풀린 곳이 없는지**가 요점이다
  - **판정**: 뒤집힌 판정에 전부 처리 결과가 붙었는가

---

## 발견

*(페이즈마다 찾은 것과 고친 것을 여기 쌓는다. 렌즈를 통과하면 "문제 없음"도 적는다)*

### B2 — 새 주장 근거 재확인 (2026-08-05) *※ B0보다 먼저 돌림*

**순서를 바꾼 이유** — 노션 MCP 토큰이 만료돼 B0·B1(노션 본문이 필요)을 못 돌린다.
B2는 **코드로만** 하는 페이즈라 먼저 했다. 노션이 열리면 B0으로 돌아간다.

**코드 근거는 전부 그대로다 — 문제 없음**

| 확인한 것 | 결과 |
|---|---|
| `TimingWheel.h`에 Windows API가 없는가 | 없다. `std::thread`(:161) · `steady_clock`(:202,207) · `sleep_for`(:209)뿐 |
| `IOCPServer.cpp:477` 등록 · `:494` 갱신 | 그 줄이 `RequestRegister` · `RequestRefresh`가 맞다 |
| `SESSION_TIMEOUT_SEC = 60` @ `IOCPServer.h:535` | 맞다 |
| "70초 뒤 무응답만 끊겼다" | 원본 `PLAN` 4-H 표(+70초 → count 1 · timed_out 1)와 같다 |
| 5-A 문단의 `localhostForwarding` 서술 | 원본 5-A와 어긋나지 않는다 |
| `_sendSubmitBusy`가 IOCP 팔에 먼저 | `main` 시점 `Transport_Iocp.cpp` **6곳**, `_sending` 선언 없음, `IOCPServer.h:103`이 "제출자가 둘인 IOCP 팔"을 지목 |
| `4500` @ `main.cpp:31` | 맞다 |
| spdlog 0건 · windows.h `LockFreeCompat.h:19` · CAS128 **11줄** | 셋 다 지금도 그대로 |

**어긋난 것 — 1건 ★ (A3의 서술이 부정확했다)**

TSan을 20회 돌려 분포를 냈다. **`setarch` 없이 돌린 결과가 한 가지가 아니다.**

| setarch 없이 20회 | 횟수 |
|---|---|
| `rc=66` · 못 뜸(`FATAL: unexpected memory mapping`) | **16** |
| `rc=66` · **정상 실행됨** | **2** |
| `rc=139` · SIGSEGV(출력 없음) | **2** |

`setarch -R` 5회는 전부 `rc=66` · `race=5`로 **일정하다.**

A3은 3회+2회만 보고 **"못 떴을 때 rc=66"으로 단정**했는데, 실제로는

- **"ASLR을 끄지 않으면 시작조차 못 한다"가 확률적이다** — 20회 중 2회는 그냥 떴다
- **실패 코드도 66만이 아니다** — SIGSEGV(139)로도 죽는다

**논지는 오히려 강해진다.** "종료 코드만으로 판정하면 안 된다"의 근거가
*"같은 값이 두 상황을 가리킨다"*에서 *"같은 명령이 뜨기도 안 뜨기도 하고, 실패 코드마저 갈린다"*로
더 단단해졌다. **고칠 곳은 단정 두 군데뿐이다.**

| 어디 | 지금 | 고칠 방향 |
|---|---|---|
| p5 2장 콜아웃 제목 | "TSan은 WSL2에서 **시작조차 못 한다**" | "**대개** 시작조차 못 한다" |
| p5 직접확인② 주석 | "# ASLR을 끄지 않으면 시작조차 못 한다" | 20회 중 18회로 수치화 |
| `LINUX_PORT_PLAN.md:259` | "이때 **종료 코드는 66**인데" | 실패 모드가 둘임을 반영 |

**→ 처리 대기.** 노션 토큰이 만료돼 지금 못 고친다. 재인증 후 **세 곳을 한꺼번에** 고친다
(노션과 원본이 따로 놀면 안 된다).

**자가검증에서 나온 것 (①)** — 분포를 처음 셀 때 **stderr를 `/dev/null`로 버려** 분류가 망가졌다
(TSan 보고는 stderr로 나간다). "정상실행 3 / 죽음 17"로 잘못 나왔고, `2>&1`로 고쳐 다시 셌다.
*A1에서 덤프 오프셋을 틀렸던 것과 같은 종류 — **내 도구부터 의심하라**는 교훈이 또 나왔다.*

**→ 처리 완료(2026-08-05)** — 노션 재인증 후 세 곳을 고쳤다.
p5 콜아웃 제목 "**대개** 시작조차 못 한다" · p5 주석 "ASLR을 켜 두면 **20회 중 18회** 못 떴다" ·
`LINUX_PORT_PLAN.md:259`에 **분포(16/2/2)를 그대로** 넣었다.
p5 콜아웃 본문("종료 코드는 66 … 구분되지 않는다")은 **논지가 맞고 문장도 틀리지 않아 그대로 뒀다** —
제목과 주석에 확률이 드러나므로 독자가 오해하지 않는다.

### B0 · B1 — 반영 대조와 통독 (2026-08-05)

fetch 한 번에 **대조와 통독을 겸했다**(같은 본문을 두 번 받을 이유가 없다). 그래서 두 페이즈를 함께 적는다.

**대상** — 5페이지를 다시 받아 전문을 읽었다(p5 · p3 · epoll · p2 · p1).
**p4는 생략했다** — 고친 3곳이 전부 A0·A1이고, **수정 직후 fetch·스크린샷으로 확인한 뒤 그 페이지는
더 손대지 않았다.** 근거 없이 건너뛴 것이 아니라 확인 이력이 있어서다.

**찾은 것 — 2건 (전부 고침). 둘 다 "노션에만" 있던 오류다 ★**

| 어디 | 노션 | 스냅샷 |
|---|---|---|
| p5 3장 (A6에서 넣은 문단) | "WSL IP가 재부팅마다 **바뀜다**" | **바뀐다** (정상) |
| p3 1장 (A6에서 넣은 콜아웃) | "**한 쪽만** 보면 몰랐을 자리다" | **한쪽만** (정상) |

**원인이 같다.** 같은 문장을 **노션에는 `\uXXXX` 이스케이프로, 스냅샷에는 파이썬에서 한글 그대로**
넣었다. 그래서 **한쪽만 틀어졌다.** `뀐`(U+B010)을 `뀜`(U+B01C)으로 쓴 것이고,
"한쪽"에는 없던 공백이 끼었다.

*A6의 자가검증은 "적어 둔데" 하나만 잡고 **같은 문단의 "바뀜다"는 놓쳤다.**
한 문단에 오류가 둘일 수 있다는 걸 전제하지 않았다 — 하나 찾으면 거기서 멈추는 습관이 문제다.*

**나머지는 문제 없음**

- **p5** — 고친 자리 9곳이 전부 정확히 있다. 위 1건 외 글자 손상 없음
- **p3** — 고친 자리 7곳(목록·콜아웃 신설·5장 제목/도입/본문·소스 지도·"적어 뒀다") 전부 정확. 위 1건 외 없음
- **epoll** — 5곳(범위 콜아웃·대응표·"세 겹"·6장 콜아웃·spdlog 주석) 전부 정확, 손상 없음
- **p2** — 2곳(windows.h 주석·"11줄") 정확, 손상 없음
- **p1** — 교훈 표 1곳 정확
- **"내가 안 한 변경" 0건** — 감사 중 사용자가 직접 고친 흔적은 없었다
- **A6의 "조용한 실패"(p3 목록)는 재시도분이 살아 있다** — 지금 "· 세션 타임아웃"이 들어가 있다

**범위 확인(③)** — 26곳 중 p4의 3곳을 뺀 **23곳을 눈으로 확인**했고, 그 과정에서 5페이지 전문을
통독했다. 통독이 실제로 일했다 — 두 발견 다 **매칭도 렌더도 정상이라 읽어야만 드러나는** 종류였다.

### B3 — 판정 재검토 · 결산 (2026-08-05)

**취소 3건 — 전부 유지**

| 취소했던 것 | 재판정 |
|---|---|
| A0 `addr2line` 들여쓰기 | 렌더 정상을 스크린샷으로 봤다. **유지** |
| A1 CAS128 5곳 | `grep -n`으로 318·343·352·429·452 확정, B2에서 11줄 재확인. **유지** |
| A2 `objdump` 2,200줄 | WSL에서 2200·193 재현. **유지** — 다만 설명은 아래 참조 |

**남긴 4건 — 전부 유지.** 코드 블록 설명 주석 · `addr2line` 예시 주소 · `char16_t` "정확히 16비트" ·
"측정 보류" 넷 다 남긴 이유가 그대로 선다. 다시 봐도 판단이 바뀌지 않았다.

**감사 기록의 수치 — 3건이 어긋났다 (전부 정정)**

이 페이즈의 요점은 *"감사가 자기 작업량을 부풀렸나"*였다. 세어 보니 **부풀린 것 하나, 줄여 적은 것 하나**가 있었다.

| 어디 | 기록 | 실제 | 왜 |
|---|---|---|---|
| A1·A7 | 줄번호 인용 **74건** | **69건** | **세는 단위가 섞였다** — `LockFreeQueue.h:318,343,352,429,452`를 한 곳에서는 백틱 1개로, 다른 곳에서는 줄번호 5개로 셌다. 백틱 기준으로 통일하면 69 |
| A6·A7 | 완료 단계 **24개** | **26개** | `- [x]` 를 눈으로 세다 둘을 빠뜨렸다. `grep -c`로 26 확정 (미완 5개까지 31) |
| A2 | "1,936은 **그때 빌드의 값**이다" | 근거 약함 | `VERIFY:99`가 세는 방법을 안 밝혔다. 같은 날 기록인데 258 차이라 **빌드 차이인지 계산법 차이인지 가릴 수 없다** — "재현되지 않는다"로 완화 |

**대조 자체는 온전했다** — A6은 26개 목록을 **보면서** 대조했고 4-H·5-A를 찾아냈다. 숫자만 틀렸다.
A1도 스크립트로 전량 추출해 맞댔으므로 누락은 없다. *센 방법을 안 적어두면 나중에 자기 기록도 못 믿는다.*

**결산 24건(고침 17 · 취소 3 · 남김 4)은 맞다** — 페이즈별로 다시 더해 확인했다(5+1+1+4+2+2+2 = 17).

---

## B 결산

**찾은 것 6건 — 전부 처리**

| 페이즈 | 무엇 | 처리 |
|---|---|---|
| B0·B1 | 노션에만 있던 글자 오류 2건 (`바뀜다` · `한 쪽만`) | 고침 |
| B2 | TSan 실패가 확률적인데 단정으로 씀 | 고침(3곳) |
| B3 | 감사 기록 수치 3건 | 정정 |

**A 감사의 판정은 하나도 안 뒤집혔다.** 고친 17건은 지금도 옳고, 취소 3건·남긴 4건도 그대로다.
B가 잡은 것은 전부 **A가 만든 부산물**이었다 — 넣다가 틀어진 글자, 표본이 얕은 단정, 잘못 센 숫자.

**같은 원인이 되풀이됐다**

- **한글 이스케이프 조립** — A에서 3번, B0·B1에서 2번 더 발견. 총 **5건**.
  노션과 스냅샷에 같은 문장을 다른 방법으로 넣은 것이 화근이었다
- **표본이 얕은 단정** — A3은 5회 보고 "rc=66"이라 했고, B2에서 20회를 돌리니 갈렸다
- **내 도구부터 의심** — A1 덤프 오프셋, B2 stderr 버림. 두 번 다 **결과가 이상하면 대상보다 도구**였다

**규칙으로 못 박은 것** — 재귀는 여기서 끊는다. **C 감사는 없다.**
남은 의심은 다음 통독 때 본다.

**판정** — 발견 6건 전부에 처리 결과가 붙었다. 관측대기 0.

### 마지막 통독 — 종결 (2026-08-05)

닫는 기준을 **"결함 0"이 아니라 "넣다가 틀어진 것 0"**으로 잡고, **B가 손댄 9곳**만 훑었다
(노션 4 — "대개" · "20회 중 18회" · "바뀐다" · "한쪽만" / 로컬 5 — `PLAN:259` · 인용 69 · 완료 26 ·
objdump 설명 · `SELF` 체크). 새 결함은 찾지 않았고 문장도 새로 쓰지 않았다.

- **넣다가 틀어진 글자·어미 0건** → 기준 충족
- **옛값 잔존 1건** — A1 범위 확인 줄의 "줄번호 **74/74**"가 안 고쳐져 있었다(→ 69/69).
  글자 손상이 아니라 **고치다 만 자리**다. 같은 값을 두 곳에 적어 둔 것이 원인
- `B2` 체크 표시가 안 눌려 있던 것도 이때 발견해 고쳤다 — 작업은 끝났는데 표시만 빠져 있었다

**종결.** 남은 의심은 다음 통독 때 본다.

---

## 이 루프를 닫는 법 (다음에 또 하게 되면)

**"결함이 없나"로 물으면 안 닫힌다.** 검사할 때마다 새로 나오는 것은 대개
**고치는 행위가 만든 것**이지, 원래 있던 결함이 아니기 때문이다. 이번이 그랬다 —
B가 찾은 6건 중 **A의 판정을 뒤집은 것은 0건**이고 전부 A가 새로 만든 부산물이었다.

닫으려면 질문에 셋을 박는다.

| | 무엇 |
|---|---|
| **범위** | 지난 라운드에서 **내가 손댄 자리만**. 전체 다시 보기 금지 |
| **판정** | **"넣다가 틀어진 것 0"** — 결함 0이 아니라 |
| **금지** | 새 결함 찾지 말 것, 고칠 게 있어도 **문장을 새로 쓰지 말 것** |

대상이 라운드마다 줄어(24 → 26곳 → 9곳) 한두 번이면 0이 된다.
**진짜 종결책은 원인 쪽**이다 — 이번 6건 중 5건이 *한글을 이스케이프로 조립한 것*이었고,
그건 이미 있던 규칙을 어겨서 생겼다. 안 어기면 다음 라운드에 나올 것이 없다.
