# 리눅스 포팅 — 완료 페이즈 재검증

이미 `- [x]`로 닫은 페이즈들을 **다시 확인**하는 문서. 진행 계획은 `LINUX_PORT_PLAN.md`가 정본이고,
이 문서는 그 기록이 **지금도 사실인지** 만 본다.

## 왜 하는가

페이즈를 닫을 때는 그 시점에 통과했지만, **뒤 작업이 앞을 깨뜨릴 수 있다.**
실제로 4-I의 회귀빌드에서 Windows 파손 3건이 나왔다 — 4-C에서 넣은 `Platform.h`의 `<WinSock2.h>`가
`sockaddr` 재정의를 일으켰고, 그건 4-D~4-H 내내 드러나지 않았다. 리눅스만 보고 있었기 때문이다.

## 검증 규칙

**한 번에 한 그룹만.** 체크가 안 된 첫 그룹을 처리하고 결과를 적는다.

각 그룹에서 보는 것은 셋이다.

1. **판정 재실행** — 그 페이즈의 판정 기준을 실제로 다시 돌린다
2. **근거 대조** — 계획서에 적은 파일·줄번호·개수·수치가 지금 코드와 맞는지 확인한다
3. **회귀 확인** — 뒤 작업이 이 페이즈의 결과를 깨뜨리지 않았는지 본다

결과는 `- [x]`로 바꾸고 **확인한 수치와 함께** 적는다. 어긋난 것이 나오면
`LINUX_PORT_PLAN.md` 쪽 기록도 함께 고친다(그쪽이 정본이므로).

### 지금은 하지 않는 것

**CPU를 오래 무는 검증은 보류한다** — PC를 쓰는 중이면 결과가 노이즈를 먹는다.
해당 항목은 `[측정보류]`로 표시했고, PC가 한가할 때 5단계 측정과 함께 몰아서 한다.

---

## V1. 환경·툴체인 (0단계)

- [x] **V1** 재확인 — **통과 (2026-08-04). 회귀 없음**

  | 항목 | 결과 |
  |---|---|
  | 툴체인 | g++ 13.3.0 / cmake 3.28.3 / ninja 1.11.1 / libmysqlclient 8.0.46 — 0단계와 동일 |
  | 형제 배치 | LockFree 헤더 5개 전부 제자리 (`LockFreeCompat.h` 포함) |
  | configure | 통과 |
  | **전체 3개 타겟 빌드** | **exit 0, 경고 0, 에러 0** (`mmo_server` 1,424,632B / `lockfree_headers_check` 62,528B / `epoll_echo_poc` 17,496B) |
  | 근거 대조 | 계획서 0-C의 `LockFreeConfig.h:39` — 실제 39행이 첫 형제경로 include, 정확 |

  0단계를 닫을 때는 타겟이 `epoll_echo_poc` 하나뿐이었는데 지금 3개다. **전부 깨끗하게 빌드된다.**
  - `cmake -S . -B build-linux -G Ninja -DCMAKE_CXX_COMPILER=g++-13` configure 통과
  - g++ 13.3 / cmake 3.28.3 / ninja / libmysqlclient 존재
  - `/mnt/c/.../MyGit` 아래 **LockFree·MMO 형제 배치** (`LockFreeConfig.h`의 상대경로 전제)
  - **회귀 관점**: 이후 CMake에 타겟이 3개 늘었다(`mmo_server`·`lockfree_headers_check`·`epoll_echo_poc`). 전부 빌드되는지

## V2. LockFree 이식 (1단계)

- [x] **V2** 재확인 — **통과 (2026-08-04). 회귀 없음**

  | 항목 | 결과 |
  |---|---|
  | 어댑터 제공 | **17종 전부 존재** (원자연산 5 · 힙 5 · 정렬 2 · TLS 4 · SwitchToThread 1) |
  | 1-A 개수 대조 | **Interlocked 18곳 / CAS128 7곳 — 계획서와 정확히 일치** |
  | `-mcx16` 방어막 | 있음 → exit 0 / 없음 → **`#error` 발동** (살아 있다) |
  | 인라인 CAS | `lockfree_headers_check` **19개** / `mmo_server` **18개**, 외부 CAS 호출 **0** |
  | libatomic 의존 | 두 산출물 모두 **0** |
  | **타입 충돌 회귀** | `LONG64`=`long long`, `ULONG`=`DWORD`=`uint32_t` — **`Platform.h`와 `LockFreeCompat.h`가 동일** |

  - **가장 중요한 확인은 `mmo_server`에도 인라인 CAS 18개가 남아 있다는 것**이다. 1단계에서 `lockfree_headers_check`로만 봤던 것이, 4단계에서 만든 서버 본체에서도 유지된다 — `__sync` 전환이 실제 서버 바이너리까지 살아 있다
  - 4-C에서 `Platform.h`에 같은 이름의 타입을 넣을 때 한 번 `conflicting declaration`이 터졌던 자리다. 지금은 양쪽이 같은 타입이라 문제없다

  > **측정 방법 실수 하나 — 처음엔 개수가 안 맞았다**
  > `*.h`로 훑었더니 Interlocked 24 / CAS128 8이 나왔다. `LockFreeCompat.h`가 같은 디렉토리에 있어 **어댑터 정의 자신이 카운트에 섞인** 것이었다. 1-A의 대상은 자료구조 헤더 4개뿐이므로 그것만 다시 세어 18/7을 확인했다. **코드가 바뀐 게 아니라 내 측정이 틀렸다.**
  - `LockFreeCompat.h`가 **어댑터 5종 + 힙 5종 + 정렬 2종 + TLS 5종**을 여전히 제공하는지
  - `-mcx16` **없이** 빌드하면 `#error`가 나는지 (방어막이 살아 있는지)
  - 산출물에 **인라인 `cmpxchg16b`**가 있고 **libatomic 호출이 0**인지
  - 계획서 1-A의 개수(Interlocked 18곳·CAS128 7곳)가 지금도 맞는지
  - **회귀 관점**: 4-C에서 `Platform.h`에도 `ULONG`/`DWORD`/`LONG64`를 넣었다 — `LockFreeCompat.h`와 **같은 타입인지**(다르면 conflicting declaration이 다시 난다)

## V3. 락프리 동작 (2단계)

- [x] **V3** 재확인 — **통과 (2026-08-04). 회귀 없음** *(전량 실행은 측정보류 유지)*

  | 항목 | 결과 |
  |---|---|
  | 일반 빌드 | exit 0, **경고 0 / 에러 0** |
  | 증폭 빌드(`USE_RACE_HOOK`) | exit 0, **경고 0 / 에러 0** |
  | 증폭 라벨 | 일반 `RACE_HOOK OFF - 대조군` / 증폭 `RACE_HOOK ON` — 분기가 살아 있다 |
  | 최소 스모크 (2스레드 1초) | `fl` 일반 rc0 · `fl` 증폭 rc0 · `cons` rc0 |
  | 인라인 CAS | 테스트 바이너리 **54개**, 외부 호출 0 |

  - 1단계를 닫은 뒤 `LockFreeCompat.h`에 **힙 5종·정렬 2종·TLS 4종·`SwitchToThread`·타입 4개**가 추가됐고, `ULONG`/`DWORD` 폭도 한 번 바뀌었다. 그 뒤에도 **테스트가 경고 없이 빌드된다**
  - 실행 부하를 최소로 둔 스모크(2스레드·1초)만 돌렸다. **6종 전량 대조는 여전히 측정보류** — PC가 한가할 때 5단계와 함께 한다
  - `[측정보류]` 테스트 6종 전량 실행 대조 — 멀티스레드 스트레스라 CPU를 오래 문다
  - 지금 할 것: **빌드만** 확인 (`TestCode.cpp` 일반/증폭 양쪽, 경고 0)
  - **회귀 관점**: 1단계 이후 `LockFreeCompat.h`가 여러 번 바뀌었다(힙·TLS·타입 추가). 테스트가 여전히 빌드되는지

## V4. CrashDump (3단계)

- [x] **V4** 재확인 — **통과 (2026-08-04). 회귀 없음**

  | 항목 | 결과 |
  |---|---|
  | `-rdynamic` | `build.ninja`에 **3회**(타겟 3개 전부) |
  | 동적 심볼 | `mmo_server` **1,936개**, CrashDump 관련 **3개** export |
  | 실제 크래시 | 종료코드 **134(SIGABRT)**, 덤프에 `signal 6` + `Reason` 기록 |
  | 이름 표시 | `CCrashDump::SignalHandler(int, siginfo_t*, void*)` · `ExternProbe_Outer()` (demangle 확인) |
  | **Windows 무영향** | `CrashDump.h`는 `main` 대비 **115줄 추가만, 삭제 0** — 기존 Windows 경로 무변경 |

  - 3-A 당시 동적 심볼은 검증 타겟 하나에서 100개였는데, 지금은 **서버 본체에서 1,936개**다. `add_link_options` 전역 적용이 새 타겟까지 덮고 있다는 뜻이다
  - 4-C에서 넣은 `WriteRaw` 헬퍼는 **리눅스 분기(302행 `#else`) 안쪽 360행**에 있다. Windows 경로 밖이라 회귀 여지가 없다
  - `-rdynamic`이 링크 옵션에 남아 있고 동적 심볼이 export되는지
  - 실제 크래시에서 **함수명 포함 스택**이 나오는지 (가볍다 — 프로세스 하나 죽이는 정도)
  - **회귀 관점**: 4-C에서 `CrashDump.h`에 `WriteRaw` 헬퍼를 넣었다. Windows 경로는 안 건드렸는지

## V5. epoll 백엔드 — 구조 (4-A ~ 4-E)

- [x] **V5** 재확인 — **통과. 다만 두 가지를 잡았다 (2026-08-04)**

  | 항목 | 결과 |
  |---|---|
  | 세션 수명 | 30회 반복 → **created 30 / destroyed 30 / count 0** |
  | 리눅스 클린 빌드 | 에러 0, **경고 2** (아래 참조) |
  | 전송 경계 | 14개 전부 중립 타입 유지 (`Platform::NetSocket`·`uint32_t`) |

  > **발견 1 — 4-B의 경계 중립화가 미완이었다**
  >
  > 경계 목록(`IOCPServer.h` 409~422)은 전부 중립인데, **`TransportSubmitSegment`(398행)만 아직 `SOCKET`을 쓰고 있었다.**
  > 4-A가 "경계 14개"를 셀 때 이 함수가 목록과 떨어진 위치에 있어 빠졌고, 그래서 4-B의 중립화 대상에도 안 들어갔다.
  > 리눅스에서 컴파일되던 이유는 `Platform.h`의 `using SOCKET = int` 별칭이 받아 줬기 때문이다 — **동작 문제는 아니지만 의도가 어긋난 상태**였다.
  > 선언 1곳 + 구현 3팔(IOCP·RIO·epoll)을 `Platform::NetSocket`으로 맞췄다. **실제 경계는 14개가 아니라 15개다.**

  > **발견 2 — V1의 "경고 0"이 헐거웠다**
  >
  > V1에서 증분 빌드로 "경고 0"을 확인했는데, **재컴파일되지 않은 TU의 경고는 애초에 출력되지 않는다.**
  > 클린 빌드로 다시 재니 **경고 2건**이 나왔다 — 둘 다 `-Winvalid-offsetof`, `InternalFreeList.h:48`.
  > `NODE`가 템플릿 `T`를 품어 standard-layout이 아닌데 `offsetof`를 쓰는 자리다. **LockFree 원본의 성질이고 이식이 만든 것이 아니다**(MSVC는 이 경고를 내지 않아 Windows에서는 안 보인다).
  > 억제하지 않고 남긴다 — 위치가 고정이라 새 경고와 구분되고, 끄면 진짜 문제를 숨길 수 있다.
  > **리눅스 기준선은 "경고 0"이 아니라 "알려진 2건"이다.**
  >
  > 교훈: **빌드 판정은 클린으로 해야 한다.** 앞으로 회귀빌드 항목은 증분으로 대신하지 말 것.
  - 리눅스 서버 빌드(`mmo_server`) 경고 0
  - **세션 수명**: 붙었다 끊기 반복에서 `created == destroyed`, `count 0` (가벼운 규모로)
  - 계획서 4-A의 분류 수치(경계 3개·골격 129건)가 지금도 맞는지
  - **회귀 관점**: 4-G에서 `_epollWantWrite`를 추가하며 `ResetTransportState`를 건드렸다 — 세션 재사용이 여전히 깨끗한지

## V6. epoll 백엔드 — 데이터 경로 (4-F ~ 4-I)

- [x] **V6** 재확인 — **통과 (2026-08-04). 기록 하나를 정밀하게 고쳤다**

  | 항목 | 결과 |
  |---|---|
  | 수신 | 패킷 20개 → `recv_packets` **0→20**, `packet_errors` **0** |
  | 송신 | 서버 `send_bytes` 증가 **526** = 클라 8개 수신 합계 **526** (유실 0) |
  | 코얼레싱 | 패킷 22 / syscall 11 — **2:1 유지** |
  | 왕복 | 이동 20회 → 응답 19개 / 채팅 → `S2C_CHAT` |
  | 타임아웃 | +45초 count 2 → **+70초 count 1, timedout 1** → 최종 0 (무응답분만 정리) |
  | 서버 에러 로그 | 0줄 |

  - **V5에서 바꾼 `TransportSubmitSegment` 시그니처가 송신을 깨뜨리지 않았다** — 바이트 수가 정확히 일치한다

  > **4-I 기록을 정밀하게 고쳤다 — 이동 응답은 `S2C_MOVE_START`가 아니다**
  >
  > 4-I에서 "이동 40회 → 클라가 31개 되받음"이라고만 적고 **패킷 종류를 확인하지 않았다.**
  > 이번에 종류를 세어 보니 전부 **`S2C_SECTOR_UPDATES`(1019)** 였다 — 개별 이동 패킷이 아니라 **섹터 단위로 묶인 것**이다.
  > 채택된 최적화(섹터 묶음 패킷, tick p99 −83%)가 그렇게 동작하도록 만든 결과이므로 **정상이다.**
  > 처음에 `S2C_MOVE_START(1002)`가 안 와서 결함을 의심했는데, **틀린 것은 서버가 아니라 내 기대 타입이었다.**
  >
  > 교훈: "몇 개 받았다"는 왕복 증거로 약하다. **무엇을 받았는지**까지 세야 한다.
  - **수신**: 패킷 N개 → `recv_packets == N`, `packet_errors 0`
  - **송신**: 클라가 받은 바이트 == 서버 `send_bytes` (유실 0), 코얼레싱 비율 확인
  - **왕복**: 접속 → 이동 → 채팅
  - **타임아웃**: 무응답 클라만 정리되는지 `[측정보류 아님, 다만 70초 소요]`
  - **회귀 관점**: 5-0에서 채팅 구조체 타입을 바꿨다 — 이동·채팅 왕복이 그대로인지

## V7. 크로스 OS·연결 (5-0, 5-A) + Windows 회귀

- [x] **V7** 재확인 — **통과 (2026-08-04). 회귀 없음**

  | 항목 | 결과 |
  |---|---|
  | **Windows IOCP 팔** 클린 재빌드 | **경고 0 / 오류 0** |
  | **Windows RIO 팔** 클린 재빌드 | **경고 0 / 오류 0** |
  | 구조체 크기 | 리눅스 `ChatChar` 2 / `C2S_CHAT` **1028** / `S2C_CHAT` **1034** (5-0과 동일) |
  | Windows 부하 클라 → 리눅스 서버 | `session_count` **10**, `recv_packets` 749, `send_bytes` 58,177, **`packet_errors` 0** |

  - **V5에서 `TransportSubmitSegment` 시그니처를 4개 파일에서 바꿨는데 Windows 양쪽 팔이 그대로 깨끗하다.** 이 검증이 곧 "지금까지의 모든 변경이 Windows를 깨뜨리지 않았는가"에 대한 답이다
  - 부하 클라는 **Windows에서 빌드된 기존 실행 파일**이다. 리눅스 서버와 58KB를 주고받으며 패킷 에러가 0이라는 것은, `char16_t` 교체 이후에도 **와이어 포맷이 그대로**임을 다시 확인해 준다
  - 양 OS `sizeof(MSG_C2S_CHAT)` == 1028, `MSG_S2C_CHAT` == 1034
  - Windows Release 재빌드 **IOCP 팔·RIO 팔 경고0 오류0** ← 가장 중요하다
  - Windows 부하 클라(기존 exe)로 리눅스 서버 접속 (소수 인원)
  - **회귀 관점**: 이 검증이 곧 "지금까지의 모든 변경이 Windows를 깨뜨리지 않았는가"다

---

## 검증에서 나온 것 — 결산 (2026-08-04)

7개 그룹 전부 통과. **코드가 깨진 곳은 없었고**, 대신 세 가지가 나왔다.

**① 실제 수정 1건 — `TransportSubmitSegment`의 `SOCKET` 잔존 (V5)**
경계 목록과 떨어진 위치에 있어 4-A의 집계에서 빠졌고, 그래서 4-B의 중립화 대상에도 안 들어갔다.
`Platform.h`의 `using SOCKET = int` 별칭이 받아 줘서 리눅스 빌드는 통과하고 있었다 — **동작은 멀쩡한데 의도만 어긋난** 상태였고, 이런 건 빌드로는 절대 안 잡힌다. 선언 1 + 구현 3팔을 맞췄다.

**② 판정 방법 정정 1건 — 증분 빌드로는 경고를 못 본다 (V5)**
V1에서 "경고 0"을 확인했지만 그건 증분 빌드였고, **재컴파일되지 않은 TU의 경고는 애초에 출력되지 않는다.**
클린 빌드로 다시 재니 경고 2건(`-Winvalid-offsetof`, LockFree 원본의 성질)이 있었다. 억제하지 않고 남겼다 —
**리눅스 기준선은 "경고 0"이 아니라 "알려진 2건"이다.** 앞으로 회귀빌드 판정은 반드시 클린으로.

**③ 기록 정밀화 1건 — 이동 응답의 실제 타입 (V6)**
4-I에 "이동 40회 → 31개 되받음"이라고만 적고 종류를 확인하지 않았다. 실제로는 전부
`S2C_SECTOR_UPDATES`(섹터 묶음)였다 — 채택된 최적화의 정상 동작이고, 틀린 것은 서버가 아니라 내 기대였다.
**"몇 개 받았다"는 왕복 증거로 약하다. 무엇을 받았는지까지 세야 한다.**

### 남은 것

- V3의 **테스트 6종 전량 대조**는 `[측정보류]` 상태다. PC가 한가할 때 5단계 측정과 함께 한다
